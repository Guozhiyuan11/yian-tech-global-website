# Production media

Place approved high-resolution source images in this directory.

Recommended naming convention:

`<slot-id>-desktop.<avif|webp>` and `<slot-id>-mobile.<avif|webp>`

Do not overwrite the reference mapping. Update `src/data/media-manifest.ts` so every asset remains traceable to its semantic slot and original reference IDs.
